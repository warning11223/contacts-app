import React from 'react';

const Error = () => {
    return (
        <div style={{display: 'flex', justifyContent: 'center', alignItems: 'center', height: '60vh', fontSize: '30px', color: 'red'}}>
            Something gone wrong, try again later 😔
        </div>
    );
};

export default Error;
